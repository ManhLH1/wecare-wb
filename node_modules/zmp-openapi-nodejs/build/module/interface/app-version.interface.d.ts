import { VersionStatus } from '../types/status';
import { BaseResult } from './response.interface';
export interface VersionList extends BaseResult {
    total: number;
    versions: Version[];
}
export interface Version {
    lastUpdated: number;
    versionId: number;
    size: number;
    entrypoint: string;
    name: string;
    description: string;
    status: VersionStatus;
}
